#include "main.h"
/**
 * _abs - Comutes the ablsolute value of an interger
 * @n: The interger to be computed
 *
 * Return: The absloute value of the interger.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
